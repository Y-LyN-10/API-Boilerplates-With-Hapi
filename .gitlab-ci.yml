image: node:latest

cache:
  paths:
    - node_modules/

before_script:
  - npm install

services:
  - mongo
  - redis

all_tests:
  script:
    - npm test

build_&_deploy_app:
  stage: build_deploy
  script:
    - node server